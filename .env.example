# StatusGuard Environment Configuration

# ============================================================
# Database Configuration
# ============================================================
# Option 1: Use SQLite (default, no setup required)
# Leave DATABASE_URL unset or empty, and SQLite will be used

# Option 2: Use PostgreSQL (set DATABASE_URL to connect to external PostgreSQL)
# DATABASE_URL=postgresql://user:password@localhost:5432/statusguard
# DATABASE_URL=postgresql://user:password@cloud-db.example.com:5432/statusguard

# SQLite path (only used if DATABASE_URL is not set)
SQLITE_PATH=./data/statusguard.db

# ============================================================
# Server Configuration
# ============================================================
SERVER_PORT=3001
SERVER_HOST=localhost

# ============================================================
# Frontend Configuration
# ============================================================
FRONTEND_URL=http://localhost:5173

# ============================================================
# Frontend API URL (for browser)
# ============================================================
VITE_API_URL=http://localhost:3001/api
